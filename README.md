# CarouselDemo

## Building and running on localhost

First install dependencies:

```sh
npm install
```

create a build:

```sh
npm run build
```

To start:

```sh
npm start
```

(will run on localhost:8080)
